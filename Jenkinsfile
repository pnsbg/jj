pipeline {
  agent {
    node {
      label 'master'
    }

  }
  stages {
    stage('artefact') {
      steps {
        ansiColor(colorMapName: 'xterm')
        echo 'eta golem bastun sam'
      }
    }

  }
}